---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: identityregistrations.
spec:
  group: ""
  names:
    kind: IdentityRegistration
    listKind: IdentityRegistrationList
    plural: identityregistrations
    shortNames:
    - idreg
    singular: identityregistration
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: |-
          IdentityRegistration represents a single declarative SPIRE
          registration entry derived exclusively from the identity compiler.
          It encodes no behavior and no policy.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IdentityRegistrationSpec mirrors compiler output exactly.
            properties:
              annotations:
                additionalProperties:
                  type: string
                description: |-
                  Optional metadata for audit and provenance.
                  Not interpreted by the controller.
                type: object
              parentId:
                description: SPIFFE ID of the parent identity.
                type: string
              selectors:
                description: Selector set binding identity to workload attributes.
                items:
                  description: Selector binds an identity to an attribute.
                  properties:
                    type:
                      type: string
                    value:
                      type: string
                  required:
                  - type
                  - value
                  type: object
                type: array
              spiffeId:
                description: Fully-qualified SPIFFE ID for the workload identity.
                type: string
              ttl:
                description: Optional SPIRE TTL (seconds). Nil means SPIRE default.
                format: int32
                type: integer
            required:
            - parentId
            - selectors
            - spiffeId
            type: object
          status:
            description: IdentityRegistrationStatus reports observed reconciliation
              state.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the identity registration's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              error:
                description: Last reconciliation error, if any.
                type: string
              lastSyncTime:
                description: RFC3339 timestamp of last successful sync.
                format: date-time
                type: string
              observedGeneration:
                description: Last observed metadata.generation.
                format: int64
                type: integer
              reconciled:
                description: True if desired state matches SPIRE state.
                type: boolean
              spireEntryId:
                description: SPIRE-assigned entry ID, if known.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
