{
  "metadata": { "name": "tempo", "namespace": "tempo", "annotations": { "threadforge.io/phase-override": "FROZEN" }, "labels": { "threadforge.io/workload-class": "dependency-gated" } },
  "spec": {
    "template": { "spec": { "containers": [ { "name": "tempo", "startupProbe": {}, "readinessProbe": {} } ] } }
  }
}
