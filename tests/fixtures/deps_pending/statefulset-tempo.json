{
  "metadata": { "name": "tempo", "namespace": "tempo", "labels": { "threadforge.io/workload-class": "dependency-gated" } },
  "spec": {
    "template": {
      "spec": {
        "containers": [
          { "name": "tempo", "startupProbe": {}, "image": "grafana/tempo:2.5.0" }
        ]
      }
    }
  }
}
