-- operator_ledger_v2 and compatibility view are Alembic-managed.
-- See Alembic revision: db/alembic/versions/a1b2c3d4e5f6_create_operator_ledger_v2_and_ingest_cursors.py
--
-- Infra-only SQL (schemas/postgres) must not declare CREATE TABLE for
-- application-owned objects. The authoritative table definition lives in
-- Alembic. This file is retained for historical/reference purposes only.

-- NOTE: Compatibility view (legacy `operator_ledger`) is created by the
-- Alembic migration above when appropriate for fresh installs.
