# GENERATED FROM platform/deploy/base/policy/kyverno-require-image-digests.yaml — DO NOT EDIT

# NOTE: Reference artifact only.
# Authoritative digest enforcement is ValidatingAdmissionPolicy `enforce-internal-registry`.
# This Kyverno policy is intentionally not included in platform/deploy/base/policy/kustomization.yaml.
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: threadforge-require-image-digests
  namespace: kyverno
  annotations:
    policies.kyverno.io/title: "ThreadForge Require Image Digests"
    policies.kyverno.io/category: "ThreadForge Supply Chain"
    policies.kyverno.io/severity: "critical"
    policies.kyverno.io/description: >-
      Deny workloads in governed namespaces unless every container image is pinned
      by digest (@sha256:...). This enforces immutable image provenance at admission.
spec:
  validationFailureAction: Enforce
  background: true
  rules:
    - name: require-digest-pinned-images
      match:
        any:
          - resources:
              kinds:
                - Pod
              namespaceSelector:
                matchExpressions:
                  - key: kubernetes.io/metadata.name
                    operator: In
                    values:
                      - threadforge
                      - threadforge-system
                      - forgesec
                      - workers
                      - redteam-test
      validate:
        message: >-
          All container images in governed namespaces must be digest pinned
          (image@sha256:<digest>). Tags-only images are denied.
        foreach:
          - list: "request.object.spec.containers[]"
            deny:
              conditions:
                any:
                  - key: "{{ regex_match('^.+@sha256:[a-f0-9]{64}$', element.image) }}"
                    operator: Equals
                    value: false
          - list: "request.object.spec.initContainers[]"
            preconditions:
              all:
                - key: "{{ request.object.spec.initContainers || `[]` | length(@) }}"
                  operator: GreaterThanOrEquals
                  value: 1
            deny:
              conditions:
                any:
                  - key: "{{ regex_match('^.+@sha256:[a-f0-9]{64}$', element.image) }}"
                    operator: Equals
                    value: false
          - list: "request.object.spec.ephemeralContainers[]"
            preconditions:
              all:
                - key: "{{ request.object.spec.ephemeralContainers || `[]` | length(@) }}"
                  operator: GreaterThanOrEquals
                  value: 1
            deny:
              conditions:
                any:
                  - key: "{{ regex_match('^.+@sha256:[a-f0-9]{64}$', element.image) }}"
                    operator: Equals
                    value: false
