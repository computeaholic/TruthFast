apiVersion: v1
kind: Namespace
metadata:
  name: observability
  labels:
    istio-injection: enabled
    threadforge.io/plane: observability
  annotations:
    description: "Observability infrastructure (Prometheus, Grafana, Loki, Jaeger)"
