apiVersion: v1
kind: Service
metadata:
  name: blackbox-minio
  namespace: observability
  labels:
    app: blackbox-minio
spec:
  ports:
    - name: http-metrics
      port: 9115
      targetPort: 9115
  selector:
    app: blackbox-minio
