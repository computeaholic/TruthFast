# ARCHIVAL — manual otel-gateway Deployment removed
# This Deployment has been intentionally archived and replaced by an operator-managed
# OpenTelemetryCollector CR (`otel-gateway`, mode: deployment) defined in
# `platform/deploy/infra/otel/policy.yaml`.
# Do NOT re-enable a Deployment; use the OpenTelemetry operator CR instead.
