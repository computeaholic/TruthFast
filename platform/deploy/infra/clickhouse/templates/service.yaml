apiVersion: v1
kind: Service
metadata:
  name: clickhouse
  namespace: threadforge-system
spec:
  type: ClusterIP
  ports:
    - name: tcp
      port: 9000
      targetPort: 9000
    - name: http
      port: 8123
      targetPort: 8123
  selector:
    app: clickhouse
