apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: reflex-gate
  namespace: monitoring
spec:
  selector:
    matchLabels:
      subsystem: reflex-gate
  namespaceSelector:
    matchNames:
      - threadforge
  podMetricsEndpoints:
    - port: metrics
      interval: 5s
