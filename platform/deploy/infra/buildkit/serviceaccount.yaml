apiVersion: v1
kind: ServiceAccount
metadata:
  name: buildkit
  namespace: build-system
  labels:
    app.kubernetes.io/name: buildkit
    app.kubernetes.io/managed-by: threadforge
# Note: credentials for registry push (docker config JSON, registry CA) should be mounted as secrets.
