apiVersion: v1
kind: Service
metadata:
  name: threadforge-api
  labels:
    app: threadforge-api
    threadforge.chaos/scrape-probe: "true"
spec:
  selector:
    app: threadforge-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
