apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: workers-policy
spec:
  podSelector:
    matchLabels:
      app: workers
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector: {}
  egress:
    - to:
        - namespaceSelector: {}
