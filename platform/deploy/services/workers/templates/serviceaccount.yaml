apiVersion: v1
kind: ServiceAccount
metadata:
  name: workers-sa
  labels:
    app: workers
  annotations:
    threadforge.io/immutable-note: "Dedicated ServiceAccount for workers; do not reuse default SA. Changes require RFC + security review."
