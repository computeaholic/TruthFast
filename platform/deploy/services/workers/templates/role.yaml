apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: workers-role
rules:
  # Minimal read permissions that a worker might need for safe operation (namespace-scoped)
  - apiGroups: [""]
    resources: ["configmaps", "secrets"]
    verbs: ["get", "list"]
