apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: argocd-base

# Argo CD bootstrap and RBAC
resources:
  - install.yaml
  - rbac.yaml

labels:
  - pairs:
      component: argocd
      app: argo-cd

commonAnnotations:
  description: "Argo CD installation (v2.10.1, manual sync)"
