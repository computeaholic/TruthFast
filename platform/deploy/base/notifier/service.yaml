apiVersion: v1
kind: Service
metadata:
  name: threadforge-notifier
  namespace: threadforge-system
  labels:
    app: threadforge-notifier
spec:
  selector:
    app: threadforge-notifier
  ports:
    - name: http
      port: 8080
      targetPort: http
